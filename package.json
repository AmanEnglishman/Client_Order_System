{
  "scripts": {
    "start": "concurrently \"cd frontend && PORT=3001 npm start\" \"bash -c 'source .venv/bin/activate && python manage.py runserver'\"",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
